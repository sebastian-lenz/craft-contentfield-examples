<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">

  <title>{{ entry is defined ? entry.title~' | ' : '' }}Content Field</title>

  <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.4.1/jquery.min.js" crossorigin="anonymous"></script>
  <script src="https://cdnjs.cloudflare.com/ajax/libs/popper.js/1.14.7/umd/popper.min.js" integrity="sha384-UO2eT0CpHqdSJQ6hJty5KVphtPhzWj9WO1clHTMGa3JDZwrnQq4sF86dIHNDz0W1" crossorigin="anonymous"></script>
  <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/js/bootstrap.min.js" integrity="sha384-JjSmVgyd0p3pXB1rRibZUAYoIIy6OrQ6VrjIEaFf/nJGzIxFDsf4x0xIM+B07jRM" crossorigin="anonymous"></script>

  <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/css/bootstrap.min.css" integrity="sha384-ggOyR0iXCbMQv3Xipma34MD+dH/1fQ784/j6cY/iJTQUOhcWr7x9JvoRxT2MZw1T" crossorigin="anonymous">
  <style>
    h1 { border-bottom: 1px solid #dee2e6; }
    h2 { margin-top: 3rem; border-bottom: 1px solid #dee2e6; }
    h3 { margin-top: 3rem; }
    .tab-content + p { margin-top: 2rem; }
  </style>
</head>
<body>

<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
  <div class="container">
    <a class="navbar-brand" href="#">Content Field</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav mr-auto">
        {% for item in menu().rootItems %}
          <li class="nav-item">
            <a{{ attr({
              class: 'nav-link' ~ (item.active ? ' active' : ''),
              href: item.url,
            }) }}>{{ item.title }}</a>
          </li>
        {% endfor %}
      </ul>
    </div>
  </div>
</nav>

<div class="container pt-4 pb-5">
  <div class="row">
    <div class="col-md-3">
      <div class="nav flex-column nav-pills">
        {% for item in menu().toc %}
          <a{{ attr({
            class: 'nav-link' ~ (item.active ? ' active' : ''),
            href: item.url,
          }) }}>{{ item.title }}</a>
        {% endfor %}
      </div>
    </div>
    <div class="offset-md-1 col-md-8">
      {% block content %}{% endblock %}
    </div>
  </div>
</div>

</body>
</html>
